//selecting with id

// let heading=document.getElementById("button"); //return
// console.dir(heading);

//selecting with class

// let headings=document.getElementsByClassName("myClass");
// console.dir(headings);

//selecting with tag
// let parahs=document.getElementsByTagName("p");
// console.dir(parahs);

// //QUERRY SELECTOR
// let firstEle=document.querySelector(".myClass"); // 1st element
// console.dir(firstEle);

// let allEle = document.querySelectorAll(".myClass");  //all elements
// console.log(allEle);

//innertext=> shows the visible text contained in a node

//textcontent=>shows all the full text(hidden) and what we have writeen in the index.html

//innerHTML=>shows the full markup (all <a> tags, etc)

//we can you use bactic like this

//heading.innerHTML=`<u>${heading.innerText}</u>`;
//now we don't have to write what actully in the heading tag

//Manipulating Attributes

//obj.getAttribute(attr)

//obj.setAttribute(attr,val)

//Manipulatin style
//using classList

//obj.classList

//classList.add() to add nwe classes

//classList.remove() to remove classes

//classList.contains() to check if clas exists

//classList.toggle() to toggle between and & remove
//agar class exist krti hogi to use hata dega or nahi hogi to use add kr dega

//object.appendChild(element)
//isse hum parent k child ander changes kr skte hai

//object.append(element)
//isse hum particular element k ander changes kr skte hai

//object.prepend(element)

//object.insertAdjacent(where,element);
// where
//   |
//   V
// beforebegin : before the targetElement
// afterbegin : just insider the targetElement , before its first child
// beforeend : just insider the targetElement, after its last child.
// afterend : after the targetElement itself



//removing elements

//obj.removeChild(element);
//obj.remove();


















// let div = document.querySelector("div");
// console.dir(div);

// let heading = document.querySelector("h1");
